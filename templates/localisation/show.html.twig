{% extends 'base.html.twig' %}

{% block title %}Localisation{% endblock %}

{% block body %}
    <h1>Localisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Addresse</th>
                <td>{{ localisation.address }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ localisation.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ localisation.longitude }}</td>
            </tr>

        </tbody>
    </table>

    {{ include('_edit_button.html.twig',{'action_path':'localisation_edit','id_value':localisation.id,'label':'Editer cette entrée'}) }}
    {{ include('localisation/_delete_form.html.twig') }}
    
    {{ include('_list_button.html.twig', {'action_path':'localisation_index','label':'Liste des localisations'}) }}

{% endblock %}
