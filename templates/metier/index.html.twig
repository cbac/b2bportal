{% extends 'base.html.twig' %}

{% block title %}Metier index{% endblock %}

{% block body %}
    <h1>Metier index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for metier in metiers %}
            <tr>
            <td>{{ metier.id }}</td>
                <td>{{ metier.nom }}</td>
                <td>
                    <a href="{{ path('metier_show', {'id': metier.id}) }}">show</a>
                    <a href="{{ path('metier_edit', {'id': metier.id}) }}">edit</a>
                </td>
            </tr>
            <tr>
            <td></td>
            <td>
            <table>
            <thead>
            <th>Type Prestation</th>
            </thead>
            <tbody>
                  {% for typePrestation in metier.typesPrestation %}
            <tr>
                <td>{{ typePrestation }}</td>
            </tr>
        {% else %}
            <tr>
                <td>no records found</td>
            </tr>
        {% endfor %}
            </tbody>
            </table>
            </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('metier_new') }}">Create new</a>
{% endblock %}
