<form method="post" action="{{ path('metier_del_typeprestation', {'id':metier.id, 'idt': type_prestation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete typeprestation' ~ type_prestation.id) }}">
    <button class="btn">Delete</button>
</form>
