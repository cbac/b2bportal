{% extends 'base.html.twig' %}

{% block title %}Edit Metier{% endblock %}

{% block body %}
    <h1>Edit Metier</h1>

    {{ include('metier/_form.html.twig', {'button_label': 'Update'}) }}
    <h2>Types de prestations</h2>
<table class="table">
    <thead>
        <tr>
            <th>NomType</th>
            <th>Description</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for type_prestation in metier.typesPrestation %}
        <tr>
            <td>{{ type_prestation.nomType }}</td>
            <td>{{ type_prestation.description }}</td>
            <td>
            {{ include('metier/_delete_typeprestation_form.html.twig') }}
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="4">no records found</td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{{ include('metier/_add_typeprestation_form.html.twig') }}
<br>
{{ include('metier/_delete_form.html.twig') }}
    <br>
    {{ include('metier/_back_to_list_button.html.twig') }}

    
{% endblock %}
