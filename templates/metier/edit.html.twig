{% extends 'base.html.twig' %}

{% block title %}Edit Metier{% endblock %}

{% block body %}
    <h1>Edit Metier</h1>

    {{ include('metier/_form.html.twig', {'button_label': 'Mise à jour'}) }}
    <h3>Types de prestations</h3>
<table class="table">
    <thead>
        <tr>
            <th>NomType</th>
            <th>Description</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for type_prestation in metier.typesPrestation %}
        <tr>
            <td>{{ type_prestation.nomType }}</td>
            <td>{{ type_prestation.description }}</td>
            <td>
            {{ include('metier/_delete_typeprestation_form.html.twig') }}
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="3">no records found</td>
        </tr>
        {% endfor %}
    </tbody>
</table>
{{ include('metier/_add_typeprestation_form.html.twig') }}
<br>
{{ include('metier/_delete_form.html.twig') }}
    <br>
    {{ include('_list_button.html.twig', {'action_path':'metier_index','label':'Liste des métiers'}) }}
  

    
{% endblock %}
