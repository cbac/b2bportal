{% extends 'base.html.twig' %}

{% block title %}TypeEvenement{% endblock %}

{% block body %}
    <h1>TypeEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_evenement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_evenement.nom }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('_edit_button.html.twig',{'action_path':'type_evenement_edit','id_value':type_evenement.id,'label':'Editer cette entrée'}) }}
    {{ include('type_evenement/_delete_form.html.twig') }}
    {{ include('_list_button.html.twig', {'action_path':'type_evenement_index','label':'Liste des événements'}) }}
    
{% endblock %}
