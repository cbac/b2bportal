{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ client.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ client.prenom }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('_edit_button.html.twig',{'action_path':'client_edit','id_value':client.id,'label':'Editer cette entrée'}) }}
    {{ include('client/_delete_form.html.twig') }}
    {{ include('_list_button.html.twig', {'action_path':'client_index','label':'Liste des clients'}) }}
    
{% endblock %}
