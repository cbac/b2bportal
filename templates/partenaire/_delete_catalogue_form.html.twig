<form method="post" action="{{ path('partenaire_delete_catalogue', {'id': partenaire.id, 'idc': cat_entry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('partenaire_delete_catalogue' ~ partenaire.id ~ cat_entry.id) }}">
    <button class="btn">Delete</button>
</form>
