<form method="post" action="{{ path('partenaire_delete', {'id': partenaire.id}) }}" onsubmit="return confirm('Attention ceci va supprimer toutes les informations correspondant à ce partenaire ! Confirmez vous ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partenaire.id) }}">
    <button class="btn">Supprimer ce partenaire</button>
</form>
