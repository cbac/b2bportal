{% extends 'base.html.twig' %}

{% block title %}Partenaire index{% endblock %}

{% block body %}
    <h1>Partenaire index</h1>
    
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for partenaire in partenaires %}
            <tr>
                <td>{{ partenaire.id }}</td>
                <td>{{ partenaire.nom }}</td>
                <td>{{ partenaire.prenom }}</td>
                <td>
                {{ include('_edit_button.html.twig', {'action_path':'partenaire_edit','label':'Editer le partenaire',
                'id_value': partenaire.id}) }}
                {{ include('_show_button.html.twig', {'action_path':'partenaire_show','label':'Détails du partenaire',
                'id_value': partenaire.id}) }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {{ include('_new_button.html.twig', {'action_path':'partenaire_new','label':'Ajouter un partenaire'}) }}
{% endblock %}